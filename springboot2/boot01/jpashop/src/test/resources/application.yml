spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop # create : 가지고 있는 엔티티를 모두 drop 후 create하고 실행시킴, create-drop : create랑 똑같은데 종료 시점에 drop 쿼리를 날려 DB를 초기화해줌 (자원 정리, 테스트에서 사용)
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace